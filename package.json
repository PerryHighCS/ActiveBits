{
    "name": "activebits",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "client",
        "server",
        "activities"
    ],
    "scripts": {
        "build": "npm run typecheck && echo 'Quick build (assumes dependencies installed). Use npm run deploy for full install+build.' && npm run build --workspace client",
        "deploy": "npm install --include=dev --workspaces --include-workspace-root && npm run build --workspace client",
        "start": "npm start --prefix server",
        "dev": "export NODE_ENV='dev' && concurrently \"npm run dev --prefix client\" \"npm run start --prefix server\"",
        "lint": "npm --workspace client run lint && npm --workspace server run lint && npm --workspace activities run lint",
        "verify:deploy": "npm install --include=dev --workspaces --include-workspace-root && npm run build --workspace client",
        "verify:server": "node scripts/verify-server.js",
        "typecheck": "npm run typecheck --workspaces --if-present",
        "test": "npm run typecheck && npm run lint && npm --workspace client test && npm --workspace server test && npm --workspace activities test && npm run verify:deploy && npm run verify:server"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "description": "",
    "dependencies": {
        "express": "^5.1.0",
        "vite": "^7.2.2",
        "ws": "^8.18.3"
    },
    "devDependencies": {
        "@types/node": "^25.2.1",
        "concurrently": "^9.1.2",
        "tsx": "^4.20.6",
        "typescript": "^5.9.3"
    }
}
