{
  "name": "activebits-server",
  "version": "1.0.0",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "start": "sh -c 'if [ -f dist/server.js ]; then node dist/server.js; else node --import tsx server.ts; fi'",
    "dev": "node --trace-warnings --import tsx server.ts",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "test": "sh -c 'set -e; files=$(find . -not -path \"./node_modules/*\" -name \"*.test.ts\" -print); if [ -z \"$files\" ]; then echo \"No server tests found\"; exit 0; fi; node --import tsx --test $files'",
    "test:nonport": "sh -c 'set -e; files=$(find . -not -path \"./node_modules/*\" -name \"*.test.ts\" ! -name \"galleryWalkRoutes.test.ts\" ! -name \"sessionStore.test.ts\" ! -name \"statusRoute.test.ts\" -print); if [ -z \"$files\" ]; then echo \"No server tests found\"; exit 0; fi; node --import tsx --test $files'",
    "typecheck": "tsc --noEmit",
    "test:valkey": "node --import tsx test-valkey.ts",
    "monitor:valkey": "node --import tsx monitor-valkey.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.2.0",
    "http-proxy-middleware": "^3.0.5",
    "ioredis": "^5.10.1",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.3",
    "@types/node": "^25.5.2",
    "@types/ws": "^8.18.1",
    "@typescript-eslint/eslint-plugin": "^8.58.0",
    "@typescript-eslint/parser": "^8.58.0",
    "eslint": "^10.2.0",
    "eslint-plugin-jsdoc": "^62.9.0",
    "globals": "^17.3.0",
    "tsx": "^4.20.6",
    "typescript": "^6.0.2"
  }
}
